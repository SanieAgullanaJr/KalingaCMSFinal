@model IEnumerable<KalingaCMSFinal.Models.ref_StrategicPriority>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StrategicPriorityNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StrategicPriorityDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StrategicPriorityNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StrategicPriorityDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StrategicPriorityID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StrategicPriorityID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StrategicPriorityID })
        </td>
    </tr>
}

</table>
