@model IEnumerable<KalingaCMSFinal.Models.ref_MajorOccupationGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MajorOccupationDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MajorOccupationDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MajorOccupationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MajorOccupationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MajorOccupationID })
        </td>
    </tr>
}

</table>
