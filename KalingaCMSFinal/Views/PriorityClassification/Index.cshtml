@model IEnumerable<KalingaCMSFinal.Models.ref_StrategicPriorityClassification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StrategicPriorityClassification)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StrategicPriorityClassification)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StratPriorityClassID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StratPriorityClassID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StratPriorityClassID })
        </td>
    </tr>
}

</table>
