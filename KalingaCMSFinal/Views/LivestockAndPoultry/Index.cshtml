@model IEnumerable<KalingaCMSFinal.Models.ref_LivestockPoultry>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LivestockPoultry)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LivestockPoultry)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LivestockPoultryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LivestockPoultryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LivestockPoultryID })
        </td>
    </tr>
}

</table>
