@model IEnumerable<KalingaCMSFinal.Models.ref_Province>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CountryID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RegionID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProvinceDistrict)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Capital)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CountryID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RegionID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProvinceDistrict)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Capital)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.provinceID }) |
                @Html.ActionLink("Details", "Details", new { id=item.provinceID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.provinceID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
