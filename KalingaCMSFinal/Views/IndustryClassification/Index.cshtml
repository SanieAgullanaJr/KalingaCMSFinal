@model IEnumerable<KalingaCMSFinal.Models.ref_IndustryClassification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IndustryClassification)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IndustryClassification)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IndustryClassID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IndustryClassID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IndustryClassID })
        </td>
    </tr>
}

</table>
