@model IEnumerable<KalingaCMSFinal.Models.ref_SourceOfFund>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SourceFundCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SourceFundDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SourceFundCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SourceFundDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SourceFundID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SourceFundID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SourceFundID })
        </td>
    </tr>
}

</table>
