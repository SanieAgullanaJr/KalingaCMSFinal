@model IEnumerable<KalingaCMSFinal.Models.PopulationByLanguageSpoken>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LanguageID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberHousehold)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.YearTaken)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LanguageID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumberHousehold)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.YearTaken)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PopLanguageID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PopLanguageID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PopLanguageID })
        </td>
    </tr>
}

</table>
