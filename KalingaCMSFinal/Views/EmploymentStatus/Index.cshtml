@model IEnumerable<KalingaCMSFinal.Models.ref_AppointmentStatus>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmpStatusCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmpStatusDescription)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmpStatusCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmpStatusDescription)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.AppointmentStatusID }) |
                @Html.ActionLink("Details", "Details", new { id=item.AppointmentStatusID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.AppointmentStatusID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
