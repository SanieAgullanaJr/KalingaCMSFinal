@model IEnumerable<KalingaCMSFinal.Models.ref_LandCoverClassification>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LandCoverClassificationDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LandCoverClassificationDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LandCoverClassID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LandCoverClassID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LandCoverClassID })
        </td>
    </tr>
}

</table>
