@model IEnumerable<KalingaCMSFinal.Models.ref_DayType>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DayTypeCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DayTypeDescription)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DayTypeCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DayTypeDescription)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.DayTypeID }) |
                @Html.ActionLink("Details", "Details", new { id=item.DayTypeID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.DayTypeID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
