@model IEnumerable<KalingaCMSFinal.Models.ref_Holiday>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HolidayDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HolidayDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DayTypeID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoOfHours)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HolidayDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HolidayDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DayTypeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NoOfHours)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.HolidayID }) |
                @Html.ActionLink("Details", "Details", new { id=item.HolidayID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.HolidayID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
