@model IEnumerable<KalingaCMSFinal.Models.ref_ProjectCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProjCategoryCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProjCategoryDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProjCategoryCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProjCategoryDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjCatID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjCatID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjCatID })
        </td>
    </tr>
}

</table>
