@model IEnumerable<KalingaCMSFinal.Models.ref_Sector>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SectorCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SectorDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SectorCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SectorDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SectorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SectorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SectorID })
        </td>
    </tr>
}

</table>
